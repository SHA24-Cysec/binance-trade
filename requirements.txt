# Security floor diverifikasi pada 2026-09-26 melalui advisory upstream.
# requests>=2.33.0 memuat perbaikan CVE-2026-25645 pada
# requests.utils.extract_zipped_paths. Flask>=3.1.3 memuat perbaikan
# GHSA-68rp-wp8r-4726/CVE-2026-27205. Werkzeug>=3.1.5 mengikuti security-fix
# release upstream. Versi final dan hash transitive dikunci di requirements.lock.
requests>=2.33.0
flask>=3.1.3
werkzeug>=3.1.5
python-dotenv>=1.0.0
# WebSocket market data (mode PAPER/LIVE hybrid). Diuji dengan 1.9.0.
websocket-client>=1.7

# --- Dependensi PENGEMBANGAN/TES (opsional untuk menjalankan bot) ---
# Jalankan tes dengan: pytest -q
pytest>=8.0
