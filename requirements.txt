# Batas bawah dinaikkan pada audit 2026-09-24 (temuan S-06):
# requests 2.32.4 menambal CVE-2024-47081 (kebocoran kredensial netrc),
# Flask 3.1.1 menambal CVE-2025-47278, dan Werkzeug dipin eksplisit karena
# beberapa advisory keamanan baru ditambal di 3.1.3-3.1.5.
# Dinaikkan lagi pada audit 2026-09-25 (temuan Q-06, dicek ke PyPI dan GitHub
# Advisory Database):
#   requests>=2.33.0 menambal CVE-2026-25645 / GHSA-gc5v-m9x4-r6x2
#     (pemakaian ulang file sementara pada requests.utils.extract_zipped_paths;
#     fungsi itu tidak dipakai repo ini, pin dinaikkan sebagai pertahanan
#     berlapis).
#   flask>=3.1.3 menambal CVE-2026-27205 / GHSA-68rp-wp8r-4726
#     (session tanpa header Vary: Cookie, tingkat LOW; dashboard tidak memakai
#     Flask session, pin dinaikkan sebagai pertahanan berlapis).
requests>=2.33.0
flask>=3.1.3
werkzeug>=3.1.5
python-dotenv>=1.0.0
# WebSocket market data (mode PAPER/LIVE hybrid). Diuji dengan 1.9.0.
websocket-client>=1.7

# --- Dependensi PENGEMBANGAN/TES (opsional untuk menjalankan bot) ---
# Jalankan tes dengan: pytest -q
pytest>=8.0
